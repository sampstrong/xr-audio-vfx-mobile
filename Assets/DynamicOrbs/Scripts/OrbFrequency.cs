using UnityEngine;

public struct OrbFrequency
{
    public int band;
    public Color color;

    public OrbFrequency(int band, Color color)
    {
        this.band = band;
        this.color = color;
    }
}
